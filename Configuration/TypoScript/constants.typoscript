<INCLUDE_TYPOSCRIPT: source="DIR:EXT:aisteaportfolio/Configuration/TypoScript/Constants" extensions="txt,typoscript">

aisteaportfolio {
    # cat = aisteaportfolio/links; type = int+; label = RootPage ID:ID der Startseite der Website
    rootPageId = 1

    # cat = aisteaportfolio/logoheader; type = string; label = Logo-Text für den Kopfbereich:Sofern kein Logo verwendet wird, wird dieser Text an Stelle des Logos angezeigt. Falls ein Logo verwendet wird, wird der Text für das alt- und das title-Attribut verwendet.
    logoText = medartis AG

    # cat = aisteaportfolio/logoheader; type = string; label = Logo-Datei:Pfad zu einer Logo Datei, z.B. fileadmin/logo.png oder EXT:my_sitepackage/Resources/Public/Images/logo.png
    #logoFile = EXT:aisteaportfolio/Resources/Public/Images/logo.png
    logoFile = fileadmin/user_upload/logo/logo.png

    # cat = aisteaportfolio/links; type = int+; label = Search plugin ID:ID des Plugins der indexed_search
    searchPluginId = 13

    # cat = aisteaportfolio/links; type = int+; label = Search Page ID:ID der Seite mit den Suchergebnissen
    searchTargetPid = 20

    # cat = aisteaportfolio/links; type = int+; label = Search Root ID:ID der Ausgangsseite für die indexed_search
    searchRootPidList = 1

    # cat = aisteaportfolio/socialmedia; type = string; label = Instagram:dein Benutzername bei Instagram
    socialmedia.instagram = yannickaister

    # cat = aisteaportfolio/socialmedia; type = string; label = Github:dein Benutzername bei Github
    socialmedia.github = AisteaTypo3
}

